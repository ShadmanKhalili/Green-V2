
{
  "name": "green-business-assessment-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencies for the Netlify serverless function.",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic --platform=browser --target=es2020 --define:process.env.NODE_ENV='\"production\"' && cp index.html dist/"
  },
  "dependencies": {
    "@google/genai": "^1.29.1",
    "@netlify/functions": "^2.6.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "esbuild": "^0.20.2"
  }
}